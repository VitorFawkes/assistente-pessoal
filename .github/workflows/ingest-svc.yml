# Builda a imagem do ingest-svc no runner do GitHub Actions e publica no
# GitHub Container Registry. Easypanel só faz `docker pull` da imagem pronta —
# não paga o custo do build local (ffmpeg + python + httpx).
#
# Imagem pública: ghcr.io/vitorfawkes/assistente-pessoal-ingest-svc
# Após o primeiro push, abrir https://github.com/users/VitorFawkes/packages
# e tornar o package PUBLIC (caso contrário easypanel precisa de creds pra pull).

name: ingest-svc image

on:
  push:
    branches: [main]
    paths:
      - "ingest-svc/**"
      - ".github/workflows/ingest-svc.yml"
  workflow_dispatch: {}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute lowercase repo owner
        id: owner
        env:
          REPO_OWNER: ${{ github.repository_owner }}
        run: |
          owner_lc=$(printf '%s' "$REPO_OWNER" | tr '[:upper:]' '[:lower:]')
          printf 'owner_lc=%s\n' "$owner_lc" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./ingest-svc
          file: ./ingest-svc/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.owner.outputs.owner_lc }}/assistente-pessoal-ingest-svc:latest
            ghcr.io/${{ steps.owner.outputs.owner_lc }}/assistente-pessoal-ingest-svc:${{ github.sha }}
          cache-from: type=gha,scope=ingest-svc
          cache-to: type=gha,mode=max,scope=ingest-svc
          platforms: linux/amd64
