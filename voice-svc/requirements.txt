# FastAPI stack
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Postgres (sem pgvector — ver /AGENTS.md raiz e db/0005_voice_samples.sql)
psycopg[binary,pool]==3.2.1

# Áudio / ML (torch/torchaudio são instalados separados no Dockerfile com index CPU-only)
speechbrain==1.0.0
soundfile==0.12.1
numpy<2.0

# speechbrain 1.0.0 importa de requests mas NÃO declara em install_requires —
# pip não puxa como transitiva. Fix explícito até subir a versão.
requests>=2.31,<3

# speechbrain 1.0.0 chama hf_hub_download(use_auth_token=...) — parâmetro
# DEPRECATED em huggingface_hub 0.23 e REMOVIDO em 0.27. Pinar abaixo.
huggingface_hub>=0.23,<0.27
