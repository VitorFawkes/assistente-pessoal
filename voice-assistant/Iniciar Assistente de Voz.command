#!/usr/bin/env bash
# Dê DOIS CLIQUES neste arquivo no Finder pra abrir o assistente de voz (modo chat).
# Na 1a vez o macOS pede permissão de microfone pro Terminal — clique Permitir.
cd "$(dirname "$0")"
exec ./run.sh
